# -*- coding: utf-8 -*-

"""Legacy build script for htgettoken.

This file only exists to support build infrastructure that hasn't
migrated to support PEP-517 (e.g. rpmbuild, dpkg-buildpackage).

All of the actual project metadata is in setup.cfg.
"""

from setuptools import setup
setup()
